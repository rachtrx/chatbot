ngrok http 80, change Twilio key

docker-compose -f docker-compose.yml up --build -d

flask run to get api key

also possible:
I will be on leave until tomorrow/ saturday
I will be on leave from Thursday until Friday 

have an acknowledge?

0 8 * * * /opt/conda/envs/chatbot/bin/python /home/app/web/azure_sync.py >> /var/log/daily_msges.log 2>&1


// send msg to principal

// create reply to yes